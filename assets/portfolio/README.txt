Drop the real client screenshots here, named exactly:

  eniebi-foundation.png
  orbit-digital.png
  cryptin.png

Until a file exists, the site shows a labelled placeholder instead of a
broken image. Filenames are referenced in index.html.

Recommended: 1600px wide, PNG or WebP, showing the top of the homepage.
